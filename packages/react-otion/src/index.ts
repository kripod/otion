// TODO: Export everything except `injectGlobal` and `detach`
// TODO: Create a `<Global css={{ /* ... */ }} />` component
export {
  createInstance,
  css,
  CSSOMInjector,
  DOMInjector,
  NoOpInjector,
  setInjector,
} from 'otion';
